import { Loader } from "@/components/index";

const DocumentLoader = () => {
  return (
    <main className="flex justify-center items-center h-screen w-screen">
      <Loader title="Loading the document" />
    </main>
  );
};

export default DocumentLoader;
